#!/bin/sh

menu="\
$(dockers ps open-webui)\n\
$(dockers ps sub-store)\n\
"

case $(printf "$menu" | menu $opt -p "docker $(systemctl is-active docker.service)" "$@") in
	*Dashboard* ) tui "lazydocker" ;;
	*Start*     ) tui "dockers start" ;;
	*Stop*      ) tui "dockers stop" ;;
esac
