#!/bin/sh

case "$(printf "WLAN\nBluetooth\nDisplays\nMount Samba\nMount USB\nUnmount USB\nPassword Manager" | dmenu)" in
	'WLAN'             ) wlanmenu ;;
	'Bluetooth'        ) btmenu ;;
	'Displays'         ) dispmenu ;;
	"Mount Samba"      ) mountsamba ;;
	'Mount USB'        ) mountusb ;;
	'Unmount USB'      ) unmountusb ;;
	'Password Manager' ) passmenu ;;
	*                  ) exit 1 ;;
esac
