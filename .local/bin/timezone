#!/bin/sh

help() {
	echo "Usage: timezone [auto|manual]"
}

case "$@" in
	auto   ) sudo timedatectl set-timezone $(curl http://ip-api.com/line/?fields=timezone) ;;
	manual ) sudo timedatectl set-timezone $(tzselect) ;;
	*      ) help ;;
esac
