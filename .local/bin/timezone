#!/bin/sh

case "$@" in
	auto   ) sudo timedatectl set-timezone $(curl https://ipapi.co/timezone) ;;
	manual ) sudo timedatectl set-timezone $(tzselect) ;;
	help   ) echo "Usage: timezone [auto|manual|help]" ;;
	*      ) timedatectl | grep zone | sed 's/^ *Time zone: //' ;;
esac
