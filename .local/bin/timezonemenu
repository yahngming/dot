#!/bin/sh

case "$LAUNCHER" in
	dmenu ) opt="-noi" ;;
esac

menu="\
 Auto detect\n\
 Manual select\n\
"

case $(printf "$menu" | menu $opt -p "$(timezone)" "$@") in
	*Auto*   ) tui "timezone auto" ;;
	*Manual* ) tui "timezone manual" ;;
esac
