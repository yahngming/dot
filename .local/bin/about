#!/bin/sh

help() {
	echo "Usage: about [battery|binds|cpu|drive|gpu|memory|pci|sensors|system|time|usb]"
}

case "$@" in
	battery ) upower -i $(upower -e | grep BAT) | bat -fpPl cpuinfo ;;
	binds   ) bindsmenu ;;
	cpu     ) lscpu | bat -fpPl cpuinfo ;;
	drive   ) lsblk | bat -fpPl conf ;;
	gpu     ) lsgpu | bat -fpPl cpuinfo ;;
	memory  ) free -h | bat -fpPl cpuinfo ;;
	pci     ) lspci | bat -fpPl cpuinfo ;;
	sensors ) sensors | bat -fpPl cpuinfo ;;
	system  ) fastfetch ;;
	time    ) timedatectl ;;
	usb     ) lsusb | bat -fpPl conf ;;
	*       ) help ;;
esac
