#!/bin/sh

case "$LAUNCHER" in
	dmenu  ) opt="-P -p [sudo]password:" ;;
	fuzzel ) opt="--dmenu --password --prompt-only=[sudo]password:" ;;
	rofi   ) opt="-dmenu -password -p [sudo]password:" ;;
	walker ) opt="--dmenu --password --placeholder [sudo]password:" ;;
esac

$LAUNCHER "$opt"
