#!/bin/sh

cd ~/.local/src/dot
git pull
cp -a ~/.local/src/dot/. ~/
chmod +x ~/.local/bin/* ~/.config/nsxiv/exec/*
rm -rf ~/.bash_history ~/.bash_logout ~/.git ~/.lesshst ~/postinstall.sh ~/README.md
cd - >/dev/null 2>&1
