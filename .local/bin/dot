#!/bin/sh

cd ~/.local/src/clash && git pull && cp ~/.local/src/clash/clash ~/.config/clash/config.yaml
cd ~/.local/src/dot && git pull && cp -a ~/.local/src/dot/. ~/
chmod +x ~/.local/bin/* 2> /dev/null
rm -rf ~/.git ~/README.md ~/postinstall.sh
cd - >/dev/null 2>&1
