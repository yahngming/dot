#!/bin/sh

source ~/.local/bin/lib

if [[ "$(date +%H:%M)" < "06:00" ]] || [[ "$(date +%H:%M)" > "18:00" ]]; then
	touch -f ~/.config/dark
else
	rm -f ~/.config/dark
fi
theme

systemctl --user start sun@autotheme.timer
systemctl --user enable sun@autotheme.timer
