#!/bin/sh

cut() {
	IFS=$2
	set -- $1
	printf '%s' "$@"
}

vol="$(wpctl get-volume @DEFAULT_AUDIO_SINK@)"
vol="${vol#'Volume: '}"
vol="${vol%' [MUTED]'}"
vol="$(printf "%.0f" "$(cut "$vol" ".")")"

notify-send -u low -h string:x-canonical-private-synchronous:volume -h int:value:{$vol}
