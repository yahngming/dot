#!/bin/sh

case "$BROWSER" in
	*chrome*     ) google-chrome-stable --new-tab "$@" ;;
	*fox*|*wolf* ) $BROWSER --new-tab --profile "$HOME/.config/$BROWSER" "$@" ;;
	*qute*       ) qutebrowser "$@" ;;
	*            ) $BROWSER "$@" ;;
esac
