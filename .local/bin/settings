#!/bin/sh

case "$MENU" in
	dmenu ) opt="-g 3 -l 6" ;;
esac

menu="\
 Bluetooth\n\
 Cleanup\n\
󰅟 Cloud\n\
 Coffee\n\
󰍹 Display\n\
 Dotfiles\n\
󰊖 Gamemode\n\
󰇧 IME\n\
󰌌 Keyboard\n\
 Passwords\n\
󰐪 Printer\n\
󰌘 Proxy\n\
󰡰 SMB\n\
󰇧 Timezone\n\
󰚰 Update\n\
󱊞 USB\n\
󰸉 Wallpaper\n\
󱚾 WLAN\n\
"

case $(printf "$menu" | $MENU $opt "$@") in
	*Bluetooth* ) btmenu ;;
	*Cleanup*   ) $TUI "sudo pacman -Rns $(pacman -Qtdq)" ;;
	*Cloud*     ) $TUI "rclone --progress copy ~/Documents disroot:/Documents" ;;
	*Coffee*    ) coffee ;;
	*Display*   ) dispmenu ;;
	*Dotfiles*  ) $TUI "dot" ;;
	*Gamemode*  ) gamemode ;;
	*IME*       ) fcitx5-configtool ;;
	*Keyboard*  ) keymap ;;
	*Passwords* ) passmenu ;;
	*Printer*   ) browser --new-tab localhost:631 ;;
	*Proxy*     ) browser --new-tab clash.razord.top ;;
	*SMB*       ) smbmenu ;;
	*Timezone*  ) sudo -A timedatectl set-timezone "$(curl --fail https://ipapi.co/timezone)" ;;
	*Update*    ) $TUI "up" ;;
	*USB*       ) usbmenu ;;
	*Wallpaper* ) wallpaper ;;
	*WLAN*      ) wlanmenu ;;
	*           ) exit 1 ;;
esac
