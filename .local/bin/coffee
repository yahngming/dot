#!/bin/sh

if [ -n "$WAYLAND_DISPLAY" ]; then
	pgrep -x hypridle && pkill -x hypridle || hypridle &
elif [ -n "$DISPLAY" ]; then
	xautolock -toggle
	case "$(xset q | grep '^\s*DPMS is' | awk '{print $3}')" in
		Disabled ) xset +dpms ;;
		Enabled  ) xset -dpms ;;
	esac
fi
