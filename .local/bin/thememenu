#!/bin/sh

menu="\
󰔎 auto\n\
 dark\n\
 light\n\
"

case $(printf "$menu" | menu -p "current:$(theme)" "$@") in
	*auto*  ) theme auto ;;
	*dark*  ) theme dark ;;
	*light* ) theme light ;;
esac
