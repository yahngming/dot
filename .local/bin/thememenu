#!/bin/sh

menu="\
󰔎 Auto switch\n\
 Dark mode\n\
 Light mode\n\
"

case $(printf "$menu" | menu -p "Change theme to:" "$@") in
	*Auto*  ) theme auto ;;
	*Dark*  ) theme dark ;;
	*Light* ) theme light ;;
esac
