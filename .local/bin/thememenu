#!/bin/sh

source ~/.local/bin/lib

case "$MENU" in
	dmenu ) opt="-noi" ;;
esac

menu="\
Dark mode\n\
Light mode\n\
Auto mode\n\
"

case $(printf "$menu" | $MENU $opt "$@") in
	*Dark*) touch -f ~/.config/dark ;;
	*Light*) rm -f ~/.config/dark ;;
	*Auto*) darkmode ;;
esac

theme
