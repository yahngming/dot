#!/bin/sh

menu="\
󰔎 auto\n\
 dark\n\
 light\n\
"

config="$HOME/.config/theme"
theme=$(awk '{print $1; exit}' "$config")

case $(printf "$menu" | menu -p "current:$theme" "$@") in
	*auto*  ) theme auto ;;
	*dark*  ) theme dark ;;
	*light* ) theme light ;;
esac
