#!/bin/sh

source ~/.local/bin/lib

case "$MENU" in
	dmenu ) opt="-noi" ;;
esac

menu="\
Auto switch\n\
Dark mode\n\
Light mode\n\
"

case $(printf "$menu" | $MENU $opt "$@") in
	*Auto*) autodark ;;
	*Dark*) touch -f ~/.config/dark ;;
	*Light*) rm -f ~/.config/dark ;;
esac

theme
