#!/bin/sh

case "$MENU" in
	dmenu ) opt="-noi" ;;
esac

menu="\
󰔎 Auto switch\n\
 Dark mode\n\
 Light mode\n\
"

case $(printf "$menu" | $MENU $opt -p "Select theme:" "$@") in
	*Auto*  ) darkmode auto ;;
	*Dark*  ) darkmode dark ;;
	*Light* ) darkmode light ;;
esac
