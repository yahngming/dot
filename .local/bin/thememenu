#!/bin/sh

menu="\
󰔎 Auto switch\n\
 Dark mode\n\
 Light mode\n\
"

config="$HOME/.config/theme"
case $(printf "$menu" | menu -p "current:" "$@") in
	*Auto*  ) theme auto ;;
	*Dark*  ) theme dark ;;
	*Light* ) theme light ;;
esac
