#!/bin/sh

screensavers=(\
	"pidof asciiquarium || $TERMINAL -F -e asciiquarium"\
	"pidof cmatrix || $TERMINAL -F -e cmatrix"\
	"pidof pipes.sh || $TERMINAL -F -e pipes.sh"\
)
index=$(($RANDOM%${#screensavers[@]}))
screensaver=${screensavers[$index]}
${screensaver}
