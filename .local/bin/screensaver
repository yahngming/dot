#!/bin/sh

screensavers=("asciiquarium" "cmatrix" "lavat" "pipes" "ttysvr")

for screensaver in "${screensavers[@]}"; do
	if pgrep -x "$screensaver" > /dev/null; then
		exit
	fi
done

$TERMINAL -F -e ${screensavers[$(($RANDOM%${#screensavers[@]}))]}
