#!/bin/sh

case "$LAUNCHER" in
	dmenu ) opt="-noi" ;;
esac

menu="\
 Preview screensaver\n\
 Turn off screensaver\n\
󰄱 Set screensaver to blank\n\
󰱊 Set screensaver to dim\n\
 Set screensaver to random\n\
"

case $(printf "$menu" | menu $opt -p "Screensaver" "$@") in
	*Preview*)
		screensaver on
		;;
	*off*)
		rm -f ~/.config/screen*
		;;
	*blank*)
		rm -f ~/.config/screen*
		touch -f ~/.config/screenblank
		;;
	*dim*)
		rm -f ~/.config/screen*
		touch -f ~/.config/screendim
		;;
	*random*)
		rm -f ~/.config/screen*
		touch -f ~/.config/screensaver
		;;
esac
