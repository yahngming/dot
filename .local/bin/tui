#!/bin/sh

case "$@" in
	*about*|dot|picker|*proxy*|tzselect|up)
		class="center"
		;;
	*telnet*)
		class="fullscreen"
		;;
esac

case "$TERMINAL" in
	foot) opt="-a $class" ;;
	*   ) opt="-c $class" ;;
esac

$TERMINAL $opt -e $SHELL -c "cat ~/.cache/wal/sequences; $@"
