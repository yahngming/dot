#!/bin/sh

case "$@" in
	*about*|*bat*|dot|*proxy*|*rclone*|*timezone*|up ) title="center" ;;
	*telnet*                                         ) title="fullscreen" ;;
esac

$TERMINAL -T $title -e $SHELL -c "cat ~/.cache/wal/sequences; $@"
