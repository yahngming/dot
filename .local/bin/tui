#!/bin/sh

case "$@" in
	*about*|dot|tzselect|up                                ) class="center" ;;
	*asciiquarium*|*matrix*|*lavat*|*telnet*|ttesvr|ttysvr ) class="fullscreen" ;;
esac

if [ "$class" == "center" ]; then
	case "$TERMINAL" in
		*foot* ) opt="-a $class" ;;
		*      ) opt="-c $class" ;;
	esac
	$TERMINAL $opt -e $SHELL -c "cat ~/.cache/wal/sequences; $@"
elif [ "$class" == "fullscreen" ]; then
	st -F -c $class -e "$@"
fi

