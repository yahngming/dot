#!/bin/sh

case "$@" in
	*about*|dot|tzselect|up)
		class="center"
		;;
	*telnet*)
		class="fullscreen"
		;;

case "$TERMINAL" in
	foot) opt="-a $class" ;;
	*   ) opt="-c $class" ;;
esac

$TERMINAL $opt -e $SHELL -c "cat ~/.cache/wal/sequences; $@"
