#!/bin/sh

if [ -n "$WAYLAND_DISPLAY" ]; then
	theme set
	locker
	idler
	fcitx5
	udiskie
	wl-paste --watch cliphist store
	rquickshare
	steam -silent -system-composer
elif [ -n "$DISPLAY" ]; then
	exit
fi
