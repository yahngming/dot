#!/bin/sh

if [ -n "$WAYLAND_DISPLAY" ]; then
	pgrep hyprlock || hyprlock &
elif [ -n "$DISPLAY" ]; then
	pgrep slock || slock &
fi

if pgrep -x "keepassxc"; then
	keepassxc --lock &
fi

pkill -f " screensaver "
