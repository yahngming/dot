#!/bin/sh

if [ -n "$WAYLAND_DISPLAY" ]; then
	pgrep swaylock || swaylock &
elif [ -n "$DISPLAY" ]; then
	pgrep slock || slock &
fi

if pgrep -x "keepassxc"; then
	keepassxc --lock &
fi

pkill -f " screensaver "
