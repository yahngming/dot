#!/bin/sh

menu="\
 Change randomly\n\
󰕰 Choose from a picker\n\
󱗺 Overlay weather forcast\n\
"

case $(printf "$menu" | menu -p "Wallpaper" "$@") in
	*random*  ) wallpaper random ;;
	*picker*  ) wallpaper picker ;;
	*weather* ) wallpaper weather ;;
esac
