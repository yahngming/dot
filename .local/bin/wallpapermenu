#!/bin/sh

case "$LAUNCHER" in
	dmenu ) opt="-noi" ;;
esac

menu="\
󰕰 Open a picker and press m to select a wallpaper manually\n\
 Select randomly\n\
"

case $(printf "$menu" | menu $opt -p "Set wallpaper:" "$@") in
	*manual* ) wallpaper manual ;;
	*random* ) wallpaper random ;;
esac
