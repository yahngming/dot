#!/bin/sh

case $(file --mime-type -Lb "$@") in
	image/*  ) nsxiv "$@" ;;
	text/*   ) bat --paging always "$@" ;;
	video/*  ) mpv "$@" ;;
	*office* ) libreoffice "$@" ;;
	*        ) /usr/bin/xdg-open "$@" ;;
esac
