#!/bin/sh

case $(file --mime-type -Lb "$@") in
	audio/*  ) mpv "$@" ;;
	image/*  ) nsxiv "$@" ;;
	text/*   ) bat --paging always "$@" ;;
	video/*  ) mpv "$@" ;;
	*office* ) libreoffice "$@" ;;
	*        ) /usr/bin/xdg-open "$@" ;;
esac
