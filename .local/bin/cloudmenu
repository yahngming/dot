#!/bin/sh

menu="\
󰬋 Disroot\n\
󰊶 Google Drive\n\
"

case $(printf "$menu" | menu -p "Cloud" "$@") in
	*Disroot* ) tui "rclone bisync disroot:/Documents ~/Documents --progress --size-only" ;;
	*Google * ) open https://drive.google.com/ ;;
esac
