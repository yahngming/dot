#!/bin/sh

case "$LAUNCHER" in
	dmenu ) opt="-noi" ;;
esac

menu="\
󰕮 System\n\
─────────
󱧦 Battery\n\
"

case $(printf "$menu" | menu $opt -p "About" "$@") in
	*Battery* ) opt="battery" ;;
	*System*  ) opt="system" ;;
esac

tui "about $opt; read -n 1 -s"
